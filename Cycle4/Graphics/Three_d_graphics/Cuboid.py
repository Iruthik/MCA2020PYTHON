def s_area(length,bredth,height):
    sur_area = 2 * ((length*bredth) + (bredth * height) + (length * height))
    return sur_area

def perimeter(length,bredth,height):
    perimeter = 4 * (length + bredth + height)
    return perimeter

